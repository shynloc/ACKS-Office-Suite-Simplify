# Office Suite 环境变量模板
# 复制本文件为 .env 并填入真实值；切勿将 .env 提交到 Git。

# 邮箱授权码（必填，用于 send_email；从邮箱服务商后台获取）
OFFICE_EMAIL_PASSWORD=

# 配置文件加密密钥（可选，用于加密配置文件；可用 `openssl rand -hex 16` 生成）
OFFICE_CONFIG_ENCRYPTION_KEY=

# 是否开启调试日志（可选，默认 false）
OFFICE_ENABLE_DEBUG_LOGGING=false
