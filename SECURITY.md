
# Office Suite 安全使用指南
**版本：1.0**
**更新日期：2025年1月15日**

---

## ⚠️ 安全第一原则
**永远不要将敏感信息（密码、授权码、密钥等）硬编码在任何配置文件、脚本或者版本控制系统中！

---

## 🛡️ 已实现的安全措施

### 1. 敏感信息存储
- ✅ **密码不存储在配置文件中** - 使用环境变量`OFFICE_EMAIL_PASSWORD`存储
- ✅ **配置文件权限** - 配置文件权限建议设置为`600`（仅所有者可读写）
- ✅ **脚本文件权限** - 敏感脚本权限设置为`600`
- ✅ **目录权限** - 存放目录权限建议设置为`700`

### 2. 输出安全
- ✅ **敏感信息掩码** - 日志和输出中密码、用户名部分自动隐藏
- ✅ **禁止记录敏感数据** - 默认关闭敏感数据日志记录
- ✅ **日志安全** - 默认不记录邮件内容和敏感信息

### 3. 配置安全
- ✅ **加密配置选项** - 支持配置文件加密
- ✅ **环境变量支持** - 使用`.env`文件存储敏感信息
- ✅ **安全提示** - 明确标注敏感配置项

---

## 📋 使用安全指南

### 1. 首次使用配置
```bash
# 1. 复制环境变量模板
cp .env.template .env

# 2. 编辑.env文件，填入真实的邮箱授权码
vim .env

# 3. 设置.env文件权限
chmod 600 .env

# 4. 安装python-dotenv（可选，推荐）
pip install python-dotenv
```

### 2. 环境变量配置说明
| 变量名 | 说明 | 必填 |
|--------|------|------|
| `OFFICE_EMAIL_PASSWORD` | 邮箱授权码 | ✅ 是 |
| `OFFICE_CONFIG_ENCRYPTION_KEY` | 配置文件加密密钥 | ❌ 可选 |
| `OFFICE_ENABLE_DEBUG_LOGGING` | 是否开启调试日志 | ❌ 可选 |

### 3. 安全最佳实践

#### ✅ 推荐做法
1. **使用环境变量** - 所有敏感信息都通过环境变量传递
2. **定期更换授权码** - 每3~6个月更换一次邮箱授权码
3. **最小权限原则** - 配置文件和脚本仅授予必要的权限
4. **定期审计** - 定期检查日志和配置，确保没有泄露
5. **使用密钥管理服务** - 生产环境建议使用专用的密钥管理系统
6. **启用登录提醒** - 开启邮箱的异地登录提醒功能

#### ❌ 禁止做法
1. **禁止硬编码密码** - 不要在任何文件中明文存储密码
2. **禁止提交敏感文件** - `.env`、配置文件绝对不能提交到Git
3. **禁止公开日志** - 日志文件中如果包含敏感信息要妥善处理
4. **禁止共享配置** - 不要将个人配置文件共享给他人
5. **禁止使用弱密码** - 授权码要足够复杂，不要使用简单密码

---

## 🔒 高级安全配置

### 配置文件加密
```bash
# 生成加密密钥
export OFFICE_CONFIG_ENCRYPTION_KEY=$(openssl rand -hex 16)

# 加密配置文件
# （加密功能可在后续版本中实现）
```

### 日志安全
```bash
# 定期清理日志
rm -f email_system.log

# 确保日志文件权限为600
chmod 600 email_system.log
```

### 临时文件清理
```bash
# 清理临时生成的文档和附件
rm -f ~/documents/email_attachments/*
```

---

## 🚨 安全应急响应

### 1. 授权码泄露
- **立即操作**：登录邮箱后台，更换授权码
- **后续操作**：检查邮箱登录记录，确认没有异常登录
- **通知相关人员**：如果涉及业务系统，通知管理员

### 2. 配置文件泄露
- **立即操作**：更换所有泄露的密码和密钥
- **后续操作**：审计系统访问日志，查找泄露原因
- **修复措施**：加强文件权限控制，使用更安全的存储方式

### 3. 异常邮件发送
- **立即操作**：临时禁用邮箱账号，防止进一步滥用
- **后续操作**：检查脚本和系统是否被入侵
- **修复措施**：加强系统安全防护，增加发送频率限制

---

## 📞 安全联系方式
**安全问题反馈**：请提交Issue
